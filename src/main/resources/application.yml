spring:
  datasource: 
    url: jdbc:mysql://localhost:3306/reservas?useSSL=false&useTimezone=true&serverTimezone=GMT
    username: root
    password: root
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update    
    properties.hibernate.dialect: org.hibernate.dialect.MySQL5InnoDBDialect
  thymeleaf:
    cache: false 
  banner:
    location: classpath:banner.txt
server:
  error:
    whitelabel:
      enabled: false
    